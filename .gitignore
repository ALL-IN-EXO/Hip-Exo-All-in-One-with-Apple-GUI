# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.env
*.log

# PyInstaller build artifacts (scripts/build_win.ps1, scripts/build_mac.sh)
.venv-build-*/
release/
*.spec

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Arduino / Teensy build artifacts
*.elf
*.hex
*.bin
build/
All_in_one_hip_controller_RL_update/test

# Local data (pulled from RPi, never commit)
data_pi/

# RPi runtime output logs (local, never commit)
RPi_Unified/output/

# GUI local data and captures
GUI_RL_update/data/
GUI_RL_update/captures/
*.mp4
GUI_RL_update/release/
GUI_RL_update/dist/
GUI_RL_update/build/
GUI_RL_update/*.spec
GUI_RL_update/.venv-build-win/
*.app
*.zip

# Model weights (large binary, track separately)
*.pt
*.pth

# Column mapping (auto-generated)
.column_mapping.json
GUI_RL_update/mapping.json

# RPi connection profiles (personal, gitignored — see tools/rpi_profiles.conf.example)
tools/rpi_profiles.conf

# BLE device name config (personal, gitignored — see ble_device_config.h.example)
3. BLE Bridge (ItsyBitsy nRF52840)/**/ble_device_config.h

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
Algorithm Reference/
